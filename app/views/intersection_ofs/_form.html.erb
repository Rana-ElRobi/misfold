<%= form_for(@intersection_of) do |f| %>
  <% if @intersection_of.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@intersection_of.errors.count, "error") %> prohibited this intersection_of from being saved:</h2>

      <ul>
      <% @intersection_of.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :neighbors %><br>
    <%= f.text_field :neighbors %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
