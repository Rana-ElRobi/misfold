<%= form_for(@intersectionneighbor) do |f| %>
  <% if @intersectionneighbor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@intersectionneighbor.errors.count, "error") %> prohibited this intersectionneighbor from being saved:</h2>

      <ul>
      <% @intersectionneighbor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :neighbor %><br>
    <%= f.text_field :neighbor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
