<%= form_for(@predictiontool) do |f| %>
  <% if @predictiontool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@predictiontool.errors.count, "error") %> prohibited this predictiontool from being saved:</h2>

      <ul>
      <% @predictiontool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
