<%= form_for(@proteinname) do |f| %>
  <% if @proteinname.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proteinname.errors.count, "error") %> prohibited this proteinname from being saved:</h2>

      <ul>
      <% @proteinname.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :isprimary %><br>
    <%= f.check_box :isprimary %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
