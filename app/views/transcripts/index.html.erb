<p id="notice"><%= notice %></p>

<h1>Listing Transcripts</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Iscolonical</th>
      <th>Sequence</th>
      <th>Refrencesequence</th>
      <th>Alternativename</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @transcripts.each do |transcript| %>
      <tr>
        <td><%= transcript.id %></td>
        <td><%= transcript.name %></td>
        <td><%= transcript.iscolonical %></td>
        <td><%= transcript.sequence %></td>
        <td><%= transcript.refrencesequence %></td>
        <td><%= transcript.alternativename %></td>
        <td><%= link_to 'Show', transcript %></td>
        <td><%= link_to 'Edit', edit_transcript_path(transcript) %></td>
        <td><%= link_to 'Destroy', transcript, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Transcript', new_transcript_path %>
